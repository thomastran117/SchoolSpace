# Controllers

Part of the [MVC](https://en.wikipedia.org/wiki/Model%E2%80%93view%E2%80%93controller) architecture design includes the controller. The controller is responsible for handing incoming requests, call the correct service/model and return it as JSON for the view layer to handle.