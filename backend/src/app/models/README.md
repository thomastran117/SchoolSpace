# Models

They contain the database model and types that the service and repository layer will used to perform business logic as needed. It is recommended that you build the model based on prisma generated models rather than define your own.
